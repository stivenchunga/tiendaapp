import { FacturacionPipe } from './facturacion.pipe';

describe('FacturacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FacturacionPipe();
    expect(pipe).toBeTruthy();
  });
});
